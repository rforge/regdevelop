\name{plscale}
\alias{plscale}
%- Also NEED an '\alias' for EACH other topic documented here.
\title{Generate Plscaleed Plotting Scale
}
\description{Generates plscaleed values and appropriate tick mark
  positions and labels for expressing a variable on a plscaleed scale,
  e.g., on log scale
}
\usage{
plscale(x, plscale = "log10", ticksat = NULL, n = NULL, logscale = NULL)
}
\arguments{
  \item{x}{data to be used in plotting
  }
  \item{plscale}{name of the function defining the plscaleed scale
  }
  \item{ticksat}{tick locations, If \code{NULL}, these locations will be
    generated by the function.
    An attribute \code{attr(..., "ticklabels")} may also be given.
  }
  \item{n}{approximate number of tick marks, as in \code{\link{pretty}}
  }
  \item{logscale}{if \code{NULL}, R's function \code{axTicks}
    will be called if the plscale is a log function.
  }
}
%%\details{
%%  ~~ If necessary, more details than the description above ~~
%%}
\value{
  The \code{x} data is returned, augmented by the following attributes:
  \itemize{
    \item{numvalues}{the plscaleed values to be used for plotting}
    \item{ticksat}{the location of tick marks (plscaleed values)}
    \item{ticklabels}{the labels for the tick marks showing the original
      scale}
    \item{plscale}{the name of the function used for the plscaleation}
  }
}
\author{Werner A. Stahel
}
\note{Besides the logarithmic plscale that is supported by core R
  graphics, any other plscaleation may be used, notably the so-called
  "first aid plscaleations".
}

%% ~Make other sections like Warning with \section{Warning }{....} ~

\seealso{
  \code{\link{axTicks}, \link{prettyscale}}
}
\examples{
  x <- 10^seq(-1,3,0.5)
  plscale(x)
  xx <- plscale(x, plscale="sqrt")
  plyx(xx)
  x <- seq(0,100,2)
  plyx(plscale(x, plscale="asinp", n=10), type="l") 
}
\keyword{dgraph}% use one of  RShowDoc("KEYWORDS")

